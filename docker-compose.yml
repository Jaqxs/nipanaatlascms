version: '3.8'

services:
  gbms-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: gbms_production
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
    restart: always
